# V2 Iteration 7 验收记录（2026-08-13）

## 本轮交付

- 调度器运行状态与心跳、最近调度/探测时间、最近错误和累计计数。
- 独立调度线程每 15 秒自动探测 ComfyUI 节点。
- 节点增加最近探测、连续失败次数和健康错误；连续 3 次失败才离线。
- ComfyUI 历史和远端队列均不存在的超时 Prompt 自动失败并释放并发槽。
- 管理员支持手动释放执行槽和重新提交任务，操作写入审计日志。
- V2 运行监控页面展示调度器、节点和活动任务，并每 5 秒刷新。
- 新增任务状态/节点组合索引，优化大队列并发槽查询。
- V2 增加按浏览器稳定哈希的灰度比例开关，并继续保留全局一键关闭。
- 编写灰度发布、数据库备份和紧急回退说明。

## 验收结果

| 检查项 | 结果 |
| --- | --- |
| 后端全量回归 | 56 passed |
| 运行监控专项测试 | 4 passed |
| 500 条排队任务状态查询 | 通过 |
| 孤儿 Prompt 自动释放 | 通过 |
| 手动释放和重新提交 | 通过 |
| 空数据库迁移 | `0007 (head)` |
| 实际数据库迁移 | `0007 (head)` |
| 前端类型检查 | 通过 |
| 前端生产构建 | 通过 |

## 尚需部署环境验证

- Safari/iOS 实体设备的媒体播放和触摸手势。
- 真实 500 个 ComfyUI 任务的长时间压力测试；自动化测试已覆盖 500 条数据库队列及 Web API 响应路径。
- 多节点故障切换需要至少两个真实 ComfyUI 节点。
